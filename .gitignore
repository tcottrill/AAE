# Prerequisites
*.d

# Object files
*.o
*.ko
*.obj
*.elf

# Linker output
*.ilk
*.map
*.exp

# Precompiled Headers
*.gch
*.pch

# Libraries
*.lib
*.a
*.la
*.lo

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.out
*.app
*.i*86
*.x86_64
*.hex

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Kernel Module Compile Results
*.mod*
*.cmd
.tmp_versions/
modules.order
Module.symvers
Mkfile.old
dkms.conf

# Visual Studio IDE cache (IntelliSense ipch, Browse.VC.db)
.vs/

# CMake build output (aae/aae.vcxproj remains the primary Windows build;
# this is the Task 5 CMake build tree added alongside it)
build-cmake/

# Linux build tree (CMake + Ninja, produced by scripts/linux/build.sh)
build-linux/

# MSBuild intermediate/output trees (aae.vcxproj and aae_core.vcxproj build here)
aae/x64/
aae/aae/x64/
aae/aae_core/
build-linux/aae
x64/Release/aae

# Flatpak build artefacts. aae.flatpak is a ~300MB bundle of the emulator plus
# every rom, artwork and sample - a build OUTPUT, not source, and it would
# dwarf the repository. The rest is flatpak-builder's own state.
aae.flatpak
flatpak-build.log
build-flatpak/
flatpak-repo/
.flatpak-builder/

# Brainstorming session mockups (superpowers visual companion)
.superpowers/
