new frame. Get key inputs. Copy to buffer.

Check keys, using checks to dictate values.

analog slop = 20%

gamekey[x]
{
val = 0 or 1 or analog 
lastval = 0 or 1 or analog
type   repeating, oneshot, 1 frame, analog
count reset (current count value)
analog min
analog max
analog step
}

Copy value to lastval. Check input type. Do things based on input type.
Joystick values override key values. Such as for repeating, check no change and set counter. Reset value based on counter changes. 

Check joystick for that key. Get joystick data for that axis/button. Compare it to the input values and adjust accordingly. If analog, scale to fit range, set value to new analog value. If analog control on binary input check slop and adjust value appropriately.

joystick[x]
axis
buttons
analog y/n
analog min
analog max
